import { MessageCircle } from 'lucide-react-native';

import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function MessagesScreen() {
  return <PlaceholderScreen icon={MessageCircle} />;
}
